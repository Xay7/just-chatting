import React from 'react';
import styles from './Tooltip.module.scss';

const Tooltip = (props) => {
    return (
        <div className={styles.Tooltip}>This is a text</div>
    )
}

export default Tooltip;